apiVersion: v1
kind: Service
metadata:
  name: vinculum-operator
spec:
  selector:
    app.kubernetes.io/name: vinculum-operator
  ports:
    - name: api
      port: 8084
      targetPort: api
    - name: probes
      port: 8083
      targetPort: probes
